version: "1.0"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: goodhamburger_api
    restart: always
    ports:
      - "5000:5000"


    networks:
      - goodnet

networks:
  goodnet: